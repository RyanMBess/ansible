---
- name: uri module demo
  hosts: localhost 
  gather_facts: false
  
  tasks:
    - set_fact:
        VARS: {foo: bar, baz: biz}

    - name: print type
      ansible.builtin.debug:
        var: VARS | type_debug

    - set_fact:
        AS_SHELL: "{{ VARS | dict2items}}"

    - name: print
      ansible.builtin.debug:
        var: AS_SHELL | type_debug
  